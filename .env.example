# Copy the variable names into your shell or secret manager; do not add secrets
# to this file. The hint-generation scripts do not load .env files themselves.
OPENAI_API_KEY=
OPENAI_BASE_URL=
OPENAI_ORG=
